#!/usr/bin/env bash
set -euxo pipefail

# cache bust 260619-01

# Install the ROI-quality package (provides `roi-classifier train` + the feature-column
# list / model API). Its pyproject pins the runtime deps.
cd /
git clone https://github.com/jkim0731/mfish-roi-classifier
cd /mfish-roi-classifier && pip install -e .
